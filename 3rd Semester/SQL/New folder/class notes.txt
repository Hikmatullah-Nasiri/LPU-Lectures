create table student24
(ID int, City varchar(20), State varchar(20), latitude decimal(2,2), Longtitude decimal(2,2));

insert into stu24
values(13,'Phoenix','AZ',33,112);
insert into stu24
values(44,'Denver','CO',40,105);

select * from stu24
	Primary key
Create table persons01(p_id number(10) PRIMARY KEY, p_name char(20), Address varchar(30));
desc persons01
	Foreign key
create table orders01(
Order_id number(20) PRIMARY KEY, Person_id number(30) references Persons01 (p_id), Address varchar (30)
);

	check constrain
create table persons02(
p_id number(10) PRIMARY KEY check (p_id<10),Address varchar (20));
desc persons02
insert into persons02 values(8,'Kabul');
insert into persons02 values(18,'Zabul');
select * from persons02
	Default constrain
create table orders02(
Order_id number(20) PRIMARY KEY, Person_id number(30) references Persons01 (p_id), Address varchar (30)
default 'LPU');
